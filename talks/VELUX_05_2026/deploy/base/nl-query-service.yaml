apiVersion: v1
kind: Service
metadata:
  name: nl-query
  labels:
    app.kubernetes.io/name: nl-query
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nl-query
  ports:
    - name: http
      port: 80
      targetPort: http
